name: torch-nns
channels:
  - anaconda
dependencies:
  # conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
  - jupyter
  - numpy
  - pip
  - ipykernel
  - pandas
  - pip:
    - scikit-learn
